// Top level C++ file

#include <iostream>
#include "game.cpp"

int main()
{
	Board board;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
	std::cout << board.placePiece(0, true) << std::endl;
}
