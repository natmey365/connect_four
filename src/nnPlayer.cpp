// C++ file defining the NNPlayer class

#incldue "nnPlayer.h"

NNPlayer::NNPlayer(Neural_Network network) : nn(network)
{

}

int NNPlayer::move(Board board)
{
	// Generate boards for each of the possible moves
	Board boards[7] = board->placePiece

	// Evaluate the positions, save evaluations in evaluations
}
